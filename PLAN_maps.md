# 灯下问道 · 关卡与地图扩展计划(PLAN_maps)

2026-09-17 立。现状:一张固定种子(7)的岛、一种关卡形式(十分钟守灯 + 无尽)、四档难度。用户要求三条路全做:随机岛势、择山起步、第二张图。

## 0. 约束(沿用)

- 引擎、战斗、调试钩子不动;Kit 命名契约不动(`Kit_<Name>`、骨架节点 `P_* / W_* / M{1-4}_*`、39 条剪辑名、发光材质名)
- 凡引《山海经》先核原文(wikisource 通行本,繁体照录),后世注疏与衍生形象不用;新内容并入 `SHANHAI_RESEARCH.md`
- 每个会话:ES module 解析(`modcheck.js`)→ 本地 8203 验收(`E.step / E.capture`)→ 改数值必重跑劫难档 → CHANGELOG / DESIGN / THEME / README → `build_dist.py` → docs → push → Pages 手动触发构建 → Artifact 同址重发
- 新增 `S` / `P` 字段必须在 `startRun` 重置;新增存档字段必须进 `saveRun / resumeRun` 与 `loadMeta` 迁移

## M1 · 岛势(随机种子岛) → v4.1

目标:每局的岛可以不同,又能复现。

- `world.js`:`generateMap(kit, scene, seed)` 里除坊市外的一切随种子变:
  - 四山地标组整体绕各自法阵旋转一个种子角(法阵本身不动,灵脉传送与小地图象限不变),并对每件地标加 ±2 丈抖动;散布本来就吃 `rng`
  - 道路弯度 `wig` 的相位吃种子(`buildRoads(seed)`,地面着色与 `roadDist` 同步重算)
  - 种子 7 必须逐位复现现在的岛(旋转角 0、抖动 0、相位同现值),老存档与宣传图不失效
- `main.js`:`rebuildWorld(seed)`——卸掉旧 `InstanceSet`(`scene.remove` + `dispose` 实例缓冲,几何体归 Kit 不销毁)、重跑 `generateMap`、重建环境粒子源 / 精卫航线 / 小地图底图 / 地面道路着色,`S.cullX = null`
- 标题页"岛势"三选:**定势**(种子 7,默认)/ **新岛**(每局随机)/ **记岛**(输入种子号);暂停页与结算页显示"岛势 #12345";战绩表记种子
- 存档:`saveRun` 记 `seed`,`resumeRun` 先 `rebuildWorld(seed)` 再恢复;山海图足迹按地标 `mark` 记,不受位置影响
- 验收:种子 7 的 `placements` 逐项与 v4.0 相同(数量 + 前 50 个坐标哈希);种子 8 / 9 地标位置不同、无地标压路或互相重叠(`obstacles` 自检);连续换 5 个种子后 `renderer.info.memory.geometries` 不涨;三个种子各跑一局 autopilot 修行档通关;存档续上局回到同一座岛

## M2 · 起山(择山起步) → v4.2

目标:同一张图五种开局,各有取舍。

- 标题页"起山"五选:青要(坊市,默认)/ 昆仑 / 发鸠 / 汤谷 / 招摇。后四项需先在该山点亮过一次法阵才可选(`META.lit[key]`,老存档按已解锁的护体机缘不补发,从零开始记)
- 规则:从某山起步 = 出生在该山法阵旁 3 丈;**开局即带该山护体功法**(不必守阵,但该山法阵本局仍可再守一次换 20 丹粟);代价:**该山精英在 0:45 现身**(常规要进山 8 s 后才醒),且开局天气取该山域候的极端(昆仑雪 / 汤谷晴夜暖 / 招摇雨 / 发鸠雾重),炼器坊远在岛心
- 引导卡在非青要起步时跳过"找炼器坊"一条改为"回青要炼器";`云游归来`落点仍是坊市灵泉
- 验收:五种起步出生点 / 护体 / 天气 / 精英现身时刻逐项核对;未解锁的山按钮禁用并写明条件;四山起步各跑一局修行档 autopilot(允许死亡,但不得早于 2:00);存档记 `startAt`

## M3 · 海外经:研究与设定 → `SHANHAI_RESEARCH.md` 第四节 + `THEME.md`

目标:第二张图"海外"的出处表,先有表再动手。

- 逐条核对《海外南经》《海外西经》《海外北经》《海外东经》(必要时《大荒经》互见),取原文、卷名、形貌、可做成规则的句子
- 拟定映射(以核对结果为准,不合原文的当场换):
  - 坊市:**君子国**(海外东经:"衣冠带剑,食兽,使二大虎在旁,其人好让不争",有薰华草朝生夕死)
  - 南:**厌火国 / 羽民国 / 不死民**;西:**巫咸国登葆山 / 女丑之尸 / 轩辕之丘**;北:**钟山烛阴 / 相柳所抵之泽 / 夸父邓林**;东:**汤谷之外的黑齿国 / 雨师妾 / 奢比尸**
  - 五妖、四精英、劫主各找一条原文立得住的;劫主首选**相柳**(海外北经:九首,蛇身,所抵厥为泽溪,其血腥不可以树五谷)
  - 拾取 / 护体 / 征兆各自找海外经里的名物与"见则"句;找不到就沿用山经那套并注明
- 产出:核对表(出处 / 原文 / 形貌 / 游戏用法 / 是否"见则")、不用清单(后世衍生)、`THEME.md` 海外一章

## M4 · 海外经:资产 → `build_kit_haiwai.py` / `kit_haiwai.glb`

- 由 `build_kit_shanhai.py` 拼接:头部 + 材质表 + helpers 原样;坊市换君子国(冠带人偶立像、二虎石像、薰华草);四域地物各 4–5 件;五妖 / 四精英 / 劫主骨架按契约名重做造型(`W_Head` 等节点必须在,相柳九首挂在 `W_Head` / `W_ArmL` / `W_ArmR` 与身上);拾取物与主角沿用
- 三角面预算:与山海 Kit 持平 ±10%;`--big` 预览裁图目检;GLB JSON 块对比 Kit 名 / 节点 / 剪辑集合与山海版逐一相同(新增地物 Kit 名另列)
- 全量构建约 7 分钟,放后台

## M5 · 海外经:世界、规则与接入 → v5.0-rc

- `MAPS = { shanjing, haiwai }`:每图一份 `districts / 地面配色 / 地标布局 / 散布表 / 域候 / 环境音 / ETYPES 名称与出处 / MINIS / 劫主文案 / PLACES / LORE / OMENS 子集`;`world.js` 的 `DISTRICTS` 改为按当前图取
- 换图 = 写 `SET.map` 后整页重载(Kit、骨架、DynSet、动画混合器都在启动时建,就地换风险大、收益小);标题页"图"二选,海外需先在山经图过山一次(`META.bossKills > 0`)
- 海外专属规则至少三条(以 M3 核对为准),例如:相柳所过成泽(劫主移动留毒沼,减速 + 持续伤)、厌火国人吐火(远程妖邪弹道留燃地)、不死民域回复加倍、烛阴"视为昼瞑为夜"(北域昼夜随劫主眼开合)
- 道藏分图:妖录 / 山海图 / 最佳战绩各按图记(`META.byMap`),老存档迁到 `shanjing`
- `build_dist.py`:两份 GLB 都内嵌,按 `SET.map` 取;单文件预计 ~6.4 MB、Artifact ~5.2 MB(上限 16 MB)

## M6 · 验收与发布 → v5.0

- 发布前清单:方向投影(D → (+,0)、W → (0,−))、四天气、弹窗互斥、清档一局、离线单文件、低画质档、两图 × 四难度 autopilot 基线(常羊只要求活过 2:00)
- 宣传:海外四域各一图 + 相柳一图 + 15 s GIF;README / CHANGELOG v5.0;标签 `haiwai-v5.0`
- CLAUDE.md 与记忆更新

## 风险

- **就地重建世界泄漏显存**:InstancedMesh 的实例缓冲要 `dispose`,材质与几何体是共享的不能动;用 `renderer.info.memory` 验
- **种子改变地标位置后压路 / 压法阵**:地标只绕法阵旋转、半径不变,原布局本来就不压法阵;压路用 `roadDist` 检一遍,压了就再转 15°
- **第二张图工作量**:M3–M5 约等于山海 S1–S4 的量;资产先保契约、再求好看,造型不到位的列进 CHANGELOG 待办,不拖发布
- **原文不够撑规则**:海外经多是"国"与"尸",异兽少;不够时用《大荒经》互见条目,仍须核原文
